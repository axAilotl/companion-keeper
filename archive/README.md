## Archive Layout

This directory stores legacy and local-only artifacts that should not be committed.

- `python-legacy/`: Archived Python implementation and helper scripts.
- `local-data/`: Local exports, extracted chat files, datasets, and generated outputs.
- `references/`: Local reference repos/checkouts used during migration.
- `misc/`: One-off local files and scratch artifacts.
